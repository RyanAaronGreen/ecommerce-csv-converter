import { defineConfig } from 'vitepress'

// https://vitepress.dev/reference/site-config
export default defineConfig({
  title: "eCommmerce CSV Converter",
  description: "An application for converting eCommerce CSV formats into other eCommerce CSV formats",
  base: '/repo/'
})
